## Student Model কী?

এই class টি হলো একটি **Entity Model Class**।

মানে:

```txt id="jlwmk1"
Java Object ⇄ Database Table
```

এই class এর মাধ্যমে Spring Boot এবং Hibernate database table create এবং manage করে।

---

# Full Code

```java id="jlwmk2"
package com.unisoftai.lms.model;

import jakarta.persistence.*;

@Entity
@Table(name = "students")
public class Student {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String name;

    private String email;

    // Getter Setter

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public String getEmail() {
        return email;
    }

    public void setEmail(String email) {
        this.email = email;
    }
}
```

---

# Step By Step Explanation

---

# 1. Package

```java id="jlwmk3"
package com.unisoftai.lms.model;
```

এটা class এর location define করে।

মানে:

```txt id="jlwmk4"
com.unisoftai.lms.model
```

package এর ভিতরে এই class আছে।

---

# 2. Import

```java id="jlwmk5"
import jakarta.persistence.*;
```

এখানে JPA annotations import করা হয়েছে।

JPA = Java Persistence API

এটা database mapping এর জন্য use হয়।

---

# 3. @Entity

```java id="jlwmk6"
@Entity
```

এটা সবচেয়ে important annotation।

মানে:

> এই class টি একটি database entity।

অর্থাৎ Spring বুঝে:

```txt id="jlwmk7"
এই class → database table হবে
```

---

# Without @Entity কী হবে?

যদি `@Entity` না দাও:

```java id="jlwmk8"
public class Student
```

তাহলে এটা শুধু normal Java class হবে।

Database table create হবে না।

---

# @Entity কী কাজ করে?

Spring automatically:

* table create করে
* column create করে
* object mapping করে

---

# Example

```java id="jlwmk9"
@Entity
public class Student
```

⇄

Database:

```sql id="jlwmka"
student table
```

---

# 4. @Table

```java id="jlwmkb"
@Table(name = "students")
```

এটা table এর custom name define করে।

মানে:

```txt id="jlwmkc"
table name হবে students
```

---

# Without @Table

যদি না দাও:

```java id="jlwmkd"
@Entity
public class Student
```

তাহলে default table name হবে:

```txt id="jlwmke"
student
```

(class name অনুযায়ী)

---

# With @Table

```java id="jlwmkf"
@Table(name = "students")
```

Database table:

```txt id="jlwmkg"
students
```

---

# 5. Class Declaration

```java id="jlwmkh"
public class Student
```

এটা Java class।

এই class এর object database row represent করে।

---

# Example

```java id="jlwmki"
Student s1 = new Student();
```

মানে:

```txt id="jlwmkj"
students table এর একটা row
```

---

# 6. @Id

```java id="jlwmkk"
@Id
```

এটা primary key define করে।

মানে:

```txt id="jlwmkl"
এই field unique identifier
```

Database:

```sql id="jlwmkm"
PRIMARY KEY
```

---

# Example

```java id="jlwmkn"
private Long id;
```

মানে:

```txt id="jlwmko"
student id
```

---

# Why Primary Key লাগে?

কারণ:

* unique row identify করতে
* update করতে
* delete করতে
* relation তৈরি করতে

---

# 7. @GeneratedValue

```java id="jlwmkp"
@GeneratedValue(strategy = GenerationType.IDENTITY)
```

এটা auto increment ID generate করে।

---

# Meaning

```txt id="jlwmkq"
1
2
3
4
5
```

automatic generate হবে।

---

# Database Equivalent

```sql id="jlwmkr"
AUTO_INCREMENT
```

(MySQL)

---

# GenerationType.IDENTITY

```java id="jlwmks"
GenerationType.IDENTITY
```

মানে:

Database automatically ID generate করবে।

---

# Example

তুমি insert করলে:

```json id="jlwmkt"
{
   "name":"Ashraful",
   "email":"ash@gmail.com"
}
```

Database automatically:

```txt id="jlwmku"
id = 1
```

generate করবে।

---

# 8. Fields

## id

```java id="jlwmkv"
private Long id;
```

Primary key।

---

## name

```java id="jlwmkw"
private String name;
```

Database column:

```txt id="jlwmkx"
name
```

---

## email

```java id="jlwmky"
private String email;
```

Database column:

```txt id="jlwmkz"
email
```

---

# Spring Automatically Column বানায়

এই field গুলা থেকে:

```java id="jlwml0"
private String name;
private String email;
```

Database columns create হবে।

---

# Resulting Database Table

```txt id="jlwml1"
students
--------------------------------
id       BIGINT
name     VARCHAR
email    VARCHAR
```

---

# 9. Getter & Setter

---

# Getter

```java id="jlwml2"
public String getName() {
    return name;
}
```

এটা value return করে।

Example:

```java id="jlwml3"
student.getName();
```

---

# Setter

```java id="jlwml4"
public void setName(String name) {
    this.name = name;
}
```

এটা value set করে।

Example:

```java id="jlwml5"
student.setName("Ashraful");
```

---

# Why Getter Setter লাগে?

কারণ Java এ:

```java id="jlwml6"
private
```

field directly access করা যায় না।

তাই method use করতে হয়।

---

# Flow Example

---

# JSON Request

```json id="jlwml7"
{
   "name":"Ashraful",
   "email":"ash@gmail.com"
}
```

---

# Spring কী করে?

Spring automatically:

```java id="jlwml8"
student.setName("Ashraful");
student.setEmail("ash@gmail.com");
```

call করে।

---

# Save করার সময়

```java id="jlwml9"
studentRepository.save(student);
```

Hibernate SQL generate করে।

---

# Generated SQL

```sql id="jlwmla"
INSERT INTO students(name,email)
VALUES('Ashraful','ash@gmail.com');
```

---

# Most Important Concept

এই class টি actually:

```txt id="jlwmlb"
Database Table এর Blueprint
```

---

# Complete Mapping

| Java            | Database       |
| --------------- | -------------- |
| Class           | Table          |
| Object          | Row            |
| Field           | Column         |
| @Entity         | Table Enable   |
| @Id             | Primary Key    |
| @GeneratedValue | Auto Increment |

---

# Visual Flow

```txt id="jlwmlc"
Student.java
      ↓
Hibernate/JPA
      ↓
students table
      ↓
MySQL
```

---

# What Hibernate Does Internally

Hibernate internally:

* SQL generate করে
* table create করে
* insert/update/delete handle করে
* object ↔ table mapping করে

---

# Advanced Annotations (Future Learning)

পরে এগুলো শিখবা:

| Annotation           | কাজ                    |
| -------------------- | ---------------------- |
| `@Column`            | Column customize       |
| `@OneToMany`         | One to Many relation   |
| `@ManyToOne`         | Many to One relation   |
| `@JoinColumn`        | Foreign key            |
| `@Transient`         | Database এ save হবে না |
| `@CreationTimestamp` | Auto created time      |
| `@UpdateTimestamp`   | Auto updated time      |

---

# Example with @Column

```java id="jlwmld"
@Column(nullable = false, unique = true)
private String email;
```

মানে:

* null allowed না
* duplicate allowed না

---

# Final Mental Model

ভাবো:

```txt id="jlwmle"
Student.java = students table design
```

আর Hibernate automatically সেই design অনুযায়ী database তৈরি করে।

# Phase 2: Web Layer Architecture (Spring Boot API Flow)

এই Phase-এ তুমি শিখবে Spring Boot API কিভাবে Request Receive করে, Process করে, Database থেকে Data আনে এবং Response Return করে।

---

# Learning Goal

Laravel এ:

```txt
Route
 ↓
Controller
 ↓
Service
 ↓
Repository
 ↓
Model
 ↓
Database
```

Spring Boot এ:

```txt
Client
  ↓
Controller
  ↓
Service
  ↓
Repository
  ↓
Entity
  ↓
Database
```

---

# Full Request Lifecycle

ধরো Browser/Postman থেকে request আসলো:

```http
GET /students/1
```

Spring Boot এর ভিতরে Flow:

```txt
Client
  ↓
DispatcherServlet
  ↓
Controller
  ↓
Service
  ↓
Repository
  ↓
PostgreSQL
  ↑
Repository
  ↑
Service
  ↑
Controller
  ↑
JSON Response
```

---

# Project Structure

```txt
src/main/java/com/unisoftai/lms

├── controller
│    └── StudentController.java
│
├── service
│    └── StudentService.java
│
├── repository
│    └── StudentRepository.java
│
├── model
│    └── Student.java
│
├── exception
│    ├── ResourceNotFoundException.java
│    └── GlobalExceptionHandler.java
│
└── LmsApplication.java
```

---

# Controller Layer

Controller API Request Receive করে।

Laravel Equivalent:

```php
Route::get('/students', [StudentController::class, 'index']);
```

Spring Boot:

```java
@RestController
@RequestMapping("/students")
public class StudentController {

}
```

---

# @RestController

Controller কে REST API Controller বানায়।

```java
@RestController
public class StudentController {

}
```

Automatically:

```java
return student;
```

↓

```json
{
   "id":1,
   "name":"Ashraful"
}
```

এ Convert হবে।

---

# @RequestMapping

Base URL Define করে।

```java
@RestController
@RequestMapping("/students")
public class StudentController {

}
```

API:

```http
/students
/students/1
/students/all
```

সব URL এর Root হবে:

```txt
/students
```

---

# HTTP Methods

---

# GET Request

Data Read করার জন্য।

```java
@GetMapping
public List<Student> getAllStudents() {
    return studentService.getAllStudents();
}
```

Request:

```http
GET /students
```

---

# GET Single Data

```java
@GetMapping("/{id}")
public Student getStudent(
        @PathVariable Long id
) {
    return studentService.getStudent(id);
}
```

Request:

```http
GET /students/5
```

Output:

```json
{
   "id":5,
   "name":"Ashraful"
}
```

---

# POST Request

New Data Create।

```java
@PostMapping
public Student createStudent(
        @RequestBody Student student
) {
    return studentService.createStudent(student);
}
```

Request:

```http
POST /students
```

Body:

```json
{
   "name":"Ashraful",
   "email":"ashraful@gmail.com"
}
```

---

# PUT Request

Update Data।

```java
@PutMapping("/{id}")
public Student updateStudent(
        @PathVariable Long id,
        @RequestBody Student student
) {
    return studentService.updateStudent(id, student);
}
```

Request:

```http
PUT /students/1
```

Body:

```json
{
   "name":"Updated Name",
   "email":"updated@gmail.com"
}
```

---

# DELETE Request

Data Delete।

```java
@DeleteMapping("/{id}")
public String deleteStudent(
        @PathVariable Long id
) {
    studentService.deleteStudent(id);
    return "Deleted";
}
```

Request:

```http
DELETE /students/1
```

---

# @PathVariable

URL থেকে Value Receive করে।

```java
@GetMapping("/{id}")
public Student getStudent(
        @PathVariable Long id
) {
    return studentService.getStudent(id);
}
```

Request:

```http
/students/10
```

Value:

```java
id = 10
```

Laravel:

```php
public function show($id)
{
}
```

---

# @RequestParam

Query Parameter Receive করে।

```java
@GetMapping("/search")
public String search(
        @RequestParam String name
) {
    return name;
}
```

Request:

```http
/students/search?name=ashraful
```

Output:

```txt
ashraful
```

Laravel:

```php
$request->query('name');
```

---

# Multiple Request Params

```java
@GetMapping("/search")
public String search(
        @RequestParam String name,
        @RequestParam Integer age
) {
    return name + " " + age;
}
```

Request:

```http
/students/search?name=ashraful&age=25
```

---

# Optional Request Param

```java
@GetMapping("/search")
public String search(
        @RequestParam(required = false)
        String name
) {
    return name;
}
```

Request:

```http
/students/search
```

Valid।

---

# @RequestBody

JSON → Java Object Conversion।

Request:

```json
{
  "name":"Ashraful",
  "email":"ashraful@gmail.com"
}
```

Controller:

```java
@PostMapping
public Student createStudent(
        @RequestBody Student student
) {
    return student;
}
```

Automatically:

```java
student.getName();
student.getEmail();
```

available হবে।

Laravel:

```php
$request->input('name');
```

---

# Service Layer

Business Logic Handle করে।

```java
@Service
public class StudentService {

    @Autowired
    private StudentRepository repository;

    public List<Student> getAllStudents() {
        return repository.findAll();
    }

}
```

---

# Why Service Layer?

❌ Bad Practice

```java
Controller
   ↓
Repository
```

✔ Industry Standard

```java
Controller
   ↓
Service
   ↓
Repository
```

কারণ Business Logic Service এ থাকে।

Example:

```java
Validate Student
Check Email
Send Email
Save Student
```

সব Service Layer এ হবে।

---

# Repository Layer

Database Communication Handle করে।

```java
@Repository
public interface StudentRepository
        extends JpaRepository<Student, Long> {

}
```

Available Methods:

```java
findAll()
findById()
save()
deleteById()
count()
existsById()
```

---

# Architecture Diagram

```txt
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Controller  │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Service    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Repository  │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ PostgreSQL  │
└─────────────┘
```

---

# Exception Handling

Industry Project এ:

❌

```java
return null;
```

Never।

---

# Custom Exception

```java
public class ResourceNotFoundException
        extends RuntimeException {

    public ResourceNotFoundException(
            String message
    ) {
        super(message);
    }
}
```

---

# Throw Exception

```java
public Student getStudent(Long id){

    return repository.findById(id)
        .orElseThrow(() ->
            new ResourceNotFoundException(
                "Student Not Found"
            )
        );
}
```

---

# Global Exception Handler

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(
        ResourceNotFoundException.class
    )
    public ResponseEntity<String>
    handleResourceNotFound(
        ResourceNotFoundException ex
    ){

        return ResponseEntity
            .status(404)
            .body(ex.getMessage());
    }

}
```

---

# What is @RestControllerAdvice?

Application এর সব Controller এর Exception Catch করে।

```txt
Controller A
Controller B
Controller C
      ↓
GlobalExceptionHandler
```

---

# What is @ExceptionHandler?

Specific Exception Handle করে।

```java
@ExceptionHandler(
      ResourceNotFoundException.class
)
```

মানে:

```java
throw new ResourceNotFoundException(...)
```

হলে এই Method Execute হবে।

---

# Error Response Example

Request:

```http
GET /students/999
```

Database এ Student নেই।

Response:

```json
{
   "message":"Student Not Found"
}
```

Status:

```http
404 NOT FOUND
```

---

# Complete Industry Flow

```txt
POST /students
       ↓
Controller
       ↓
Validate Request
       ↓
Service
       ↓
Business Logic
       ↓
Repository
       ↓
PostgreSQL
       ↑
Repository
       ↑
Service
       ↑
Controller
       ↑
JSON Response
```

---

# Laravel vs Spring Boot

| Laravel           | Spring Boot           |
| ----------------- | --------------------- |
| Route             | @RequestMapping       |
| Controller        | @RestController       |
| Request           | @RequestBody          |
| Route Param       | @PathVariable         |
| Query Param       | @RequestParam         |
| Service           | Service               |
| Repository        | Repository            |
| Model             | Entity                |
| Migration         | Hibernate/JPA         |
| Exception Handler | @RestControllerAdvice |

---

# Important Annotations

| Annotation            | Purpose                    |
| --------------------- | -------------------------- |
| @RestController       | REST API Controller        |
| @RequestMapping       | Base URL                   |
| @GetMapping           | GET Request                |
| @PostMapping          | POST Request               |
| @PutMapping           | PUT Request                |
| @DeleteMapping        | DELETE Request             |
| @PathVariable         | URL Value                  |
| @RequestParam         | Query Parameter            |
| @RequestBody          | JSON → Object              |
| @Service              | Business Logic             |
| @Repository           | Database Access            |
| @RestControllerAdvice | Global Exception Handler   |
| @ExceptionHandler     | Specific Exception Handler |

---

# Phase 2 Master Checklist ✅

* [ ] @RestController
* [ ] @RequestMapping
* [ ] @GetMapping
* [ ] @PostMapping
* [ ] @PutMapping
* [ ] @DeleteMapping
* [ ] @PathVariable
* [ ] @RequestParam
* [ ] @RequestBody
* [ ] Service Layer
* [ ] Repository Layer
* [ ] Exception Handling
* [ ] @RestControllerAdvice
* [ ] @ExceptionHandler
* [ ] Full Request Lifecycle
* [ ] Laravel ↔ Spring Boot Mapping

**Next Phase (Phase 3): Data Layer & JPA/Hibernate Deep Dive**

* Entity Lifecycle
* JPA Repository
* Custom Queries
* JPQL
* Native SQL
* Relationships (OneToOne, OneToMany, ManyToMany)
* Fetch Types
* Cascade Types
* Transaction Management (`@Transactional`)

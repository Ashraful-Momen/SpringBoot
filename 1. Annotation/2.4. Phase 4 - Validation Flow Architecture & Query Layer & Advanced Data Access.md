তুমি আসলে **Phase 5 কে দুই ভাগে ভাগ করো**, কারণ Industry-তে এগুলো দুইটা আলাদা Domain:

# Phase 5A: Validation Flow Architecture

```txt
Request Validation
DTO Validation
Bean Validation
Global Validation Handling
```

# Phase 5B: Query Layer & Advanced Data Access

```txt
JPQL
@Query
Native Query
Projection
Pagination
Sorting
Specification
Criteria API
Dynamic Search
Query Optimization
Indexing
Execution Plan
Performance Tuning
```

---

# Phase 5A: Validation Flow Architecture

Validation হলো Backend Security-এর First Line of Defense।

---

# Learning Goal

বোঝতে হবে:

```txt
Client Request
      ↓
DTO
      ↓
Validation
      ↓
Controller
      ↓
Service
      ↓
Repository
      ↓
Database
```

---

# Why Validation?

User Send করলো:

```json
{
   "name":"",
   "email":"abc",
   "age":-50
}
```

Validation না থাকলে:

```txt
Bad Data
      ↓
Database
```

Store হয়ে যাবে।

---

# Validation Architecture

```txt
Request
   ↓
DTO
   ↓
@Valid
   ↓
Bean Validation
   ↓
Controller
   ↓
Service
   ↓
Repository
```

---

# DTO (Data Transfer Object)

Industry Standard:

❌ Entity Direct Request এ Use করা হয় না।

```java
@PostMapping
public Student save(
     @RequestBody Student student
)
```

---

✔ DTO Use করা হয়।

```java
@PostMapping
public Student save(
     @RequestBody StudentRequest dto
)
```

---

# Create DTO

```java
public class StudentRequest {

    private String name;

    private String email;

    private Integer age;

}
```

---

# @Valid

Validation Trigger করে।

```java
@PostMapping
public Student createStudent(
      @Valid
      @RequestBody
      StudentRequest request
){
    return service.create(request);
}
```

---

# Validation Flow

```txt
Request
   ↓
DTO
   ↓
@Valid
   ↓
Validation Pass?
      ↓
 YES → Controller
 NO  → Exception
```

---

# @NotBlank

Empty String Allow করবে না।

```java
@NotBlank(
   message =
   "Name is required"
)
private String name;
```

---

Valid:

```json
{
   "name":"Ashraful"
}
```

---

Invalid:

```json
{
   "name":""
}
```

---

# @NotNull

Null Allow করবে না।

```java
@NotNull
private Integer age;
```

---

Valid:

```json
{
   "age":25
}
```

---

Invalid:

```json
{
   "age":null
}
```

---

# @Email

Email Format Check।

```java
@Email(
   message =
   "Invalid Email"
)
private String email;
```

---

Valid:

```txt
abc@gmail.com
```

---

Invalid:

```txt
abc
```

---

# @Size

String Length Check।

```java
@Size(
   min = 3,
   max = 50
)
private String name;
```

---

Valid:

```txt
Ashraful
```

---

Invalid:

```txt
A
```

---

# @Min

Minimum Value।

```java
@Min(18)
private Integer age;
```

---

Valid:

```txt
25
```

---

Invalid:

```txt
10
```

---

# @Max

Maximum Value।

```java
@Max(60)
private Integer age;
```

---

Valid:

```txt
50
```

---

Invalid:

```txt
100
```

---

# Complete DTO Example

```java
public class StudentRequest {

    @NotBlank
    @Size(min=3,max=50)
    private String name;

    @Email
    @NotBlank
    private String email;

    @NotNull
    @Min(18)
    @Max(60)
    private Integer age;

}
```

---

# Invalid Request

```json
{
   "name":"",
   "email":"abc",
   "age":10
}
```

---

# Validation Errors

```txt
Name is required

Invalid Email

Age must be >=18
```

---

# Global Validation Handler

```java
@RestControllerAdvice
public class ValidationHandler {

    @ExceptionHandler(
      MethodArgumentNotValidException.class
    )
    public Map<String,String>
    handleValidation(
      MethodArgumentNotValidException ex
    ){

       Map<String,String> errors
          = new HashMap<>();

       ex.getBindingResult()
         .getFieldErrors()
         .forEach(error -> {

           errors.put(
              error.getField(),
              error.getDefaultMessage()
           );

         });

       return errors;
    }

}
```

---

# Validation Response

```json
{
   "name":"Name is required",
   "email":"Invalid Email",
   "age":"Must be greater than 18"
}
```

---

# Phase 5A Architecture

```txt
Request
   ↓
DTO
   ↓
@Valid
   ↓
Bean Validation
   ↓
Controller
   ↓
Service
   ↓
Repository
   ↓
Database
```

---

# Phase 5B: Query Layer & Advanced Data Access

---

# Learning Goal

বোঝতে হবে:

```txt
Repository
      ↓
JPQL
      ↓
Hibernate
      ↓
SQL
      ↓
Database
```

---

# JPQL

Java Entity Based Query Language।

---

SQL:

```sql
SELECT *
FROM students
```

---

JPQL:

```java
SELECT s
FROM Student s
```

Notice:

```txt
Table না

Entity Name Use হয়
```

---

# @Query

Custom JPQL লিখতে।

```java
@Query(
"""
SELECT s
FROM Student s
WHERE s.email = :email
"""
)
Student findByEmail(
     String email
);
```

---

# Native Query

Direct SQL।

```java
@Query(
 value =
 "SELECT * FROM students",
 nativeQuery = true
)
List<Student> getAll();
```

---

# JPQL vs Native Query

| JPQL         | Native        |
| ------------ | ------------- |
| Entity Based | Table Based   |
| Portable     | DB Specific   |
| Recommended  | Special Cases |

---

# Projection

Only Required Data Return।

---

Entity:

```java
Student
```

Contains:

```txt
id
name
email
password
address
phone
```

---

Need Only:

```txt
name
email
```

---

Projection:

```java
public interface StudentView {

    String getName();

    String getEmail();

}
```

---

Repository:

```java
List<StudentView>
findAllProjectedBy();
```

---

Benefit:

```txt
Less Memory

Less Data Transfer

Better Performance
```

---

# Pagination

Large Data Split করে।

---

Without:

```txt
100000 Rows
```

---

With Pagination:

```txt
Page 1
20 Rows

Page 2
20 Rows
```

---

Repository:

```java
Page<Student> findAll(
    Pageable pageable
);
```

---

Controller:

```java
@GetMapping
public Page<Student> getAll(
      Pageable pageable
){
   return repo.findAll(pageable);
}
```

---

Request:

```http
?page=0&size=20
```

---

# Sorting

```http
?page=0
&size=20
&sort=name,asc
```

---

Programmatically:

```java
Sort.by("name")
```

---

# Specification

Dynamic Query Builder।

---

Problem:

```txt
Search By Name

Search By Email

Search By Age

Search By Status
```

100 Query লিখতে হবে।

---

Solution:

```java
Specification<Student>
```

---

Repository

```java
extends
JpaSpecificationExecutor<Student>
```

---

Flow

```txt
Filters
   ↓
Specification
   ↓
Dynamic Query
```

---

# Criteria API

Specification-এর Internal Engine।

---

Example

```java
criteriaBuilder.equal(
    root.get("name"),
    "Ashraful"
)
```

---

Used For:

```txt
Dynamic Search

Filtering

Advanced Query
```

---

# Dynamic Search Architecture

```txt
Request Filters
      ↓
Specification
      ↓
Criteria API
      ↓
JPQL
      ↓
SQL
      ↓
Database
```

---

# Query Optimization

Senior-Level Topic।

---

Bad Query:

```sql
SELECT *
FROM students
```

Need Only:

```txt
name
email
```

---

Good Query:

```sql
SELECT
name,
email
FROM students
```

---

# Indexing

Database Performance Booster।

---

Without Index

```txt
1 Million Rows

Full Table Scan
```

---

With Index

```txt
Direct Lookup
```

---

SQL

```sql
CREATE INDEX
idx_student_email
ON students(email);
```

---

Best Candidates

```txt
email

username

foreign_key

status
```

---

# Execution Plan

Database কিভাবে Query Execute করবে।

---

PostgreSQL

```sql
EXPLAIN ANALYZE

SELECT *
FROM students
WHERE email='abc@gmail.com';
```

---

Output:

```txt
Index Scan

Seq Scan

Cost

Execution Time
```

---

# Performance Tuning Checklist

### Always

✔ Pagination

✔ Projection

✔ Index

✔ LAZY Loading

✔ Join Fetch

✔ Specification

---

### Avoid

❌ SELECT *

❌ EAGER Everywhere

❌ N+1 Queries

❌ Full Table Scan

❌ Loading Entire Entity

---

# Complete Query Architecture

```txt
Controller
      ↓
Service
      ↓
Repository
      ↓

JPQL
@Query
Native Query

      ↓

Specification

      ↓

Criteria API

      ↓

Hibernate

      ↓

SQL

      ↓

Index

      ↓

Execution Plan

      ↓

Database
```

---

# Phase 5 Master Checklist ✅

## Validation Layer

* [ ] DTO
* [ ] @Valid
* [ ] @NotBlank
* [ ] @NotNull
* [ ] @Email
* [ ] @Size
* [ ] @Min
* [ ] @Max
* [ ] Global Validation Handler
* [ ] Validation Architecture

## Query Layer

* [ ] JPQL
* [ ] @Query
* [ ] Native Query
* [ ] Projection
* [ ] Pagination
* [ ] Sorting
* [ ] Specification
* [ ] Criteria API
* [ ] Dynamic Search
* [ ] Query Optimization
* [ ] Indexing
* [ ] Execution Plan
* [ ] Performance Tuning

# Next Phase (Phase 6: Spring Security & JWT Architecture)

```txt
Authentication

Authorization

Spring Security

Security Filter Chain

JWT

Access Token

Refresh Token

Role Based Access Control

UserDetailsService

Password Encoder

Custom Authentication Filter

Stateless Security

Security Architecture
```

এটাই Spring Boot Enterprise Backend-এর Security Foundation।


=====================================================================================
Excellent idea. For **Phase 5A (Validation)** and **Phase 5B (Advanced Query Layer)**, what is usually missing in notes is:

✅ Complete ASCII Architecture
✅ End-to-End Coding Flow
✅ Request → DTO → Validation → DB Flow
✅ Real Repository Examples
✅ Industry Standard Package Structure

---

# Phase 5A Complete Validation Flow (Industry Standard)

## Full Architecture

```txt
Client (Postman / Frontend)
            │
            ▼
      JSON Request
            │
            ▼
        DTO Class
            │
            ▼
         @Valid
            │
            ▼
    Bean Validation
            │
            ▼
Validation Passed?
       │      │
       │      │
      YES     NO
       │      │
       ▼      ▼
 Controller   Exception
       │           │
       ▼           ▼
   Service   GlobalExceptionHandler
       │
       ▼
 Repository
       │
       ▼
 PostgreSQL
```

---

# Step 1: Create DTO

```java
package com.unisoftai.lms.dto;

import jakarta.validation.constraints.*;

public class StudentRequest {

    @NotBlank(message = "Name is required")
    @Size(min = 3, max = 50)
    private String name;

    @Email(message = "Invalid email")
    @NotBlank
    private String email;

    @NotNull
    @Min(18)
    @Max(60)
    private Integer age;

    // Getter Setter
}
```

---

# Step 2: Controller

```java
@RestController
@RequestMapping("/students")
public class StudentController {

    @Autowired
    private StudentService service;

    @PostMapping
    public Student createStudent(

            @Valid
            @RequestBody
            StudentRequest request

    ){

        return service.createStudent(request);

    }
}
```

---

# Step 3: Service

```java
@Service
public class StudentService {

    @Autowired
    private StudentRepository repository;

    public Student createStudent(
            StudentRequest request
    ){

        Student student = new Student();

        student.setName(
                request.getName()
        );

        student.setEmail(
                request.getEmail()
        );

        return repository.save(student);
    }
}
```

---

# Step 4: Repository

```java
@Repository
public interface StudentRepository
        extends JpaRepository<Student, Long> {

}
```

---

# Step 5: Global Validation Handler

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(
            MethodArgumentNotValidException.class
    )
    public ResponseEntity<Map<String,String>>
    handleValidationException(

            MethodArgumentNotValidException ex

    ){

        Map<String,String> errors
                = new HashMap<>();

        ex.getBindingResult()
                .getFieldErrors()
                .forEach(error -> {

                    errors.put(
                            error.getField(),
                            error.getDefaultMessage()
                    );

                });

        return ResponseEntity
                .badRequest()
                .body(errors);
    }
}
```

---

# Request Flow Example

Request

```json
{
  "name":"",
  "email":"abc",
  "age":10
}
```

↓

DTO

```java
StudentRequest
```

↓

Validation

```java
@NotBlank
@Email
@Min
```

↓

Validation Failed

↓

Response

```json
{
   "name":"Name is required",
   "email":"Invalid email",
   "age":"must be greater than or equal to 18"
}
```

---

# Complete Validation Coding Flow

```txt
POST /students
      │
      ▼
StudentController

      │

      ▼
@Valid

      │

      ▼
StudentRequest DTO

      │

      ▼
Validation Rules

      │

      ▼
Pass ?

 │          │
 │          │
YES         NO
 │          │
 ▼          ▼

Service   ExceptionHandler

 │

 ▼

Repository

 │

 ▼

Database
```

---

# Phase 5B Complete Query Layer Architecture

---

# Full Architecture

```txt
Controller
    │
    ▼
Service
    │
    ▼
Repository
    │
    ▼

JPQL
@Query
Native Query

    │
    ▼

Hibernate

    │
    ▼

SQL

    │
    ▼

PostgreSQL
```

---

# Step 1: Entity

```java
@Entity
@Table(name="students")
public class Student {

    @Id
    @GeneratedValue(
      strategy =
      GenerationType.IDENTITY
    )
    private Long id;

    private String name;

    private String email;

    private Integer age;

}
```

---

# Step 2: Repository

## Derived Query

```java
@Repository
public interface StudentRepository
extends JpaRepository<Student,Long> {

    Student findByEmail(
            String email
    );

}
```

Hibernate Generates:

```sql
SELECT *
FROM students
WHERE email = ?
```

Automatically.

---

# Step 3: JPQL

```java
@Query(
"""
SELECT s
FROM Student s
WHERE s.age > :age
"""
)
List<Student> findAdultStudents(
        Integer age
);
```

JPQL Flow

```txt
Repository
      │
      ▼
JPQL

SELECT s
FROM Student s

      │

      ▼

Hibernate

      │

      ▼

SQL

SELECT *
FROM students
```

---

# Step 4: Native Query

```java
@Query(

 value=
 "SELECT * FROM students",

 nativeQuery=true

)
List<Student> getAllStudents();
```

---

# Pagination Flow

Repository

```java
Page<Student>
findAll(Pageable pageable);
```

---

Controller

```java
@GetMapping
public Page<Student> getAll(

      Pageable pageable

){

   return repository
          .findAll(pageable);

}
```

---

Request

```http
/students?page=0&size=10
```

---

Flow

```txt
100000 Records

        │

        ▼

Pageable

        │

        ▼

10 Records

        │

        ▼

Response
```

---

# Sorting Flow

Request

```http
/students?page=0
&size=10
&sort=name,asc
```

---

SQL

```sql
ORDER BY name ASC
```

Automatically.

---

# Projection Flow

Problem

```txt
Student Entity

id
name
email
phone
address
password
```

Need only:

```txt
name
email
```

---

Projection

```java
public interface StudentView {

    String getName();

    String getEmail();

}
```

---

Repository

```java
List<StudentView>
findAllProjectedBy();
```

---

Flow

```txt
Database

id
name
email
phone
password

      │

      ▼

Projection

      │

      ▼

name
email
```

---

# Specification Flow

Dynamic Search

Request

```http
/students
?name=ashraful
&age=25
```

---

Flow

```txt
Request Filter

       │

       ▼

Specification

       │

       ▼

Criteria API

       │

       ▼

Dynamic SQL

       │

       ▼

Database
```

---

Repository

```java
@Repository
public interface StudentRepository

extends JpaRepository<Student,Long>,

JpaSpecificationExecutor<Student>{

}
```

---

Specification

```java
public static
Specification<Student>
hasName(String name){

   return (root,query,cb)
      -> cb.equal(
           root.get("name"),
           name
      );

}
```

---

# N+1 Problem Example

Bad

```java
List<Department> departments
=
repository.findAll();
```

↓

Loop

```java
department.getEmployees()
```

↓

SQL

```txt
1 Query

Departments

+
50 Queries

Employees
```

↓

51 Queries

---

# Solution

```java
@Query("""
SELECT d
FROM Department d
JOIN FETCH d.employees
""")
```

---

# Query Optimization Architecture

```txt
Controller
      │
      ▼
Repository
      │
      ▼
JPQL
      │
      ▼
Hibernate
      │
      ▼
Execution Plan
      │
      ▼
Index Scan
      │
      ▼
Database
```

---

# Indexing

Without Index

```txt
1 Million Rows

       │

       ▼

Sequential Scan

       │

       ▼

Slow
```

---

With Index

```txt
1 Million Rows

       │

       ▼

B-Tree Index

       │

       ▼

Direct Lookup

       │

       ▼

Fast
```

---

# Execution Plan

PostgreSQL

```sql
EXPLAIN ANALYZE

SELECT *
FROM students
WHERE email =
'abc@gmail.com';
```

---

Look For

```txt
Seq Scan

Index Scan

Cost

Execution Time

Rows
```

---

# Complete Enterprise Query Flow

```txt
Frontend

    │

    ▼

Controller

    │

    ▼

Service

    │

    ▼

Repository

    │

    ▼

Specification

    │

    ▼

Criteria API

    │

    ▼

JPQL

    │

    ▼

Hibernate

    │

    ▼

SQL

    │

    ▼

Index

    │

    ▼

Execution Plan

    │

    ▼

PostgreSQL
```

---

# Industry Project Structure

```txt
src/main/java/com/unisoftai/lms

├── controller
│     └── StudentController
│
├── dto
│     ├── StudentRequest
│     └── StudentResponse
│
├── service
│     └── StudentService
│
├── repository
│     └── StudentRepository
│
├── specification
│     └── StudentSpecification
│
├── exception
│     └── GlobalExceptionHandler
│
├── model
│     └── Student
│
└── config
```

This is the complete beginner-to-intermediate industry flow for **Validation + Advanced Query Layer**, showing exactly how data travels from Request → DTO → Validation → Controller → Service → Repository → Hibernate → PostgreSQL and back.
===============================================================================================
Yes. For all future phases, start with:

1. **Project Structure**
2. **ASCII Architecture**
3. **Request Flow**
4. **Step-by-Step Coding**
5. **Industry Notes**
6. **Interview Notes**
7. **Master Checklist**

This makes the notes look like a complete Spring Boot roadmap.

---

# Phase 5A: Validation Flow Architecture (Complete Industry Note)

---

# Project Structure

```txt
src/main/java/com/unisoftai/lms

├── controller
│     └── StudentController.java
│
├── dto
│     ├── StudentRequest.java
│     └── StudentResponse.java
│
├── service
│     └── StudentService.java
│
├── repository
│     └── StudentRepository.java
│
├── specification
│     └── StudentSpecification.java
│
├── exception
│     └── GlobalExceptionHandler.java
│
├── model
│     └── Student.java
│
└── config
```

---

# Validation Architecture

```txt
Client
  │
  ▼

JSON Request

  │
  ▼

StudentRequest DTO

  │
  ▼

@Valid

  │
  ▼

Bean Validation

  │
  ▼

Validation Passed ?

   │           │
   │           │

 YES          NO

   │           │

   ▼           ▼

Controller   Validation Exception

   │           │

   ▼           ▼

Service   GlobalExceptionHandler

   │

   ▼

Repository

   │

   ▼

PostgreSQL
```

---

# Why Validation?

Without Validation:

```json
{
   "name":"",
   "email":"abc",
   "age":10
}
```

Bad Data →

```txt
Database
```

---

# Step 1: Add Dependency

```xml
<dependency>
    <groupId>
        org.springframework.boot
    </groupId>

    <artifactId>
        spring-boot-starter-validation
    </artifactId>
</dependency>
```

---

# Step 2: Create DTO

## StudentRequest.java

```java
package com.unisoftai.lms.dto;

import jakarta.validation.constraints.*;

public class StudentRequest {

    @NotBlank(
      message = "Name is required"
    )
    @Size(
      min = 3,
      max = 50
    )
    private String name;

    @NotBlank
    @Email(
      message = "Invalid Email"
    )
    private String email;

    @NotNull
    @Min(18)
    @Max(60)
    private Integer age;

}
```

---

# Validation Annotations

## @NotBlank

```java
@NotBlank
private String name;
```

Checks:

```txt
null
""
"     "
```

All Invalid

---

## @NotNull

```java
@NotNull
private Integer age;
```

Checks:

```txt
null
```

Only

---

## @Email

```java
@Email
private String email;
```

Valid:

```txt
abc@gmail.com
```

Invalid:

```txt
abc
```

---

## @Size

```java
@Size(
 min=3,
 max=50
)
```

---

## @Min

```java
@Min(18)
```

---

## @Max

```java
@Max(60)
```

---

# Step 3: Controller

## StudentController.java

```java
@RestController
@RequestMapping("/students")
public class StudentController {

    @Autowired
    private StudentService service;

    @PostMapping
    public Student createStudent(

        @Valid
        @RequestBody
        StudentRequest request

    ){

        return service
                .createStudent(
                    request
                );
    }
}
```

---

# Step 4: Service

## StudentService.java

```java
@Service
public class StudentService {

    @Autowired
    private StudentRepository repository;

    public Student createStudent(

         StudentRequest request

    ){

        Student student =
                new Student();

        student.setName(
            request.getName()
        );

        student.setEmail(
            request.getEmail()
        );

        student.setAge(
            request.getAge()
        );

        return repository.save(
            student
        );
    }
}
```

---

# Step 5: Repository

## StudentRepository.java

```java
@Repository
public interface StudentRepository
extends JpaRepository<Student,Long>{

}
```

---

# Step 6: Global Exception Handler

## GlobalExceptionHandler.java

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(
      MethodArgumentNotValidException.class
    )
    public ResponseEntity<Map<String,String>>
    handleValidation(

      MethodArgumentNotValidException ex

    ){

        Map<String,String> errors =
            new HashMap<>();

        ex.getBindingResult()
          .getFieldErrors()
          .forEach(error -> {

             errors.put(
                error.getField(),
                error.getDefaultMessage()
             );

          });

        return ResponseEntity
               .badRequest()
               .body(errors);
    }
}
```

---

# Complete Request Flow

```txt
POST /students

      │

      ▼

StudentController

      │

      ▼

@Valid

      │

      ▼

StudentRequest DTO

      │

      ▼

Validation

      │

      ▼

Passed ?

 │           │

 ▼           ▼

YES          NO

 │           │

 ▼           ▼

Service    ExceptionHandler

 │

 ▼

Repository

 │

 ▼

PostgreSQL
```

---

# Invalid Response Example

Request:

```json
{
  "name":"",
  "email":"abc",
  "age":10
}
```

Response:

```json
{
   "name":"Name is required",
   "email":"Invalid Email",
   "age":"must be greater than or equal to 18"
}
```

---

# Phase 5B: Query Layer & Advanced Data Access

---

# Project Structure

```txt
src/main/java/com/unisoftai/lms

├── controller
│     └── StudentController.java
│
├── dto
│     ├── StudentRequest.java
│     └── StudentResponse.java
│
├── service
│     └── StudentService.java
│
├── repository
│     └── StudentRepository.java
│
├── specification
│     └── StudentSpecification.java
│
├── exception
│     └── GlobalExceptionHandler.java
│
├── model
│     └── Student.java
│
└── config
```

---

# Complete Query Architecture

```txt
Frontend

   │

   ▼

Controller

   │

   ▼

Service

   │

   ▼

Repository

   │

   ▼

JPQL
@Query
Native Query

   │

   ▼

Specification

   │

   ▼

Criteria API

   │

   ▼

Hibernate

   │

   ▼

SQL

   │

   ▼

Index

   │

   ▼

Execution Plan

   │

   ▼

PostgreSQL
```

---

# Step 1: Entity

## Student.java

```java
@Entity
@Table(name="students")
public class Student {

    @Id
    @GeneratedValue(
      strategy=
      GenerationType.IDENTITY
    )
    private Long id;

    private String name;

    private String email;

    private Integer age;
}
```

---

# Step 2: Repository

## Derived Query Method

```java
@Repository
public interface StudentRepository
extends JpaRepository<Student,Long>{

    Student findByEmail(
         String email
    );

}
```

Spring Automatically Generates:

```sql
SELECT *
FROM students
WHERE email = ?
```

---

# Step 3: JPQL

```java
@Query(
"""
SELECT s
FROM Student s
WHERE s.age >= :age
"""
)
List<Student>
findAdultStudents(
     Integer age
);
```

---

# JPQL Flow

```txt
Repository

    │

    ▼

JPQL

SELECT s
FROM Student s

    │

    ▼

Hibernate

    │

    ▼

SQL

SELECT *
FROM students
```

---

# Step 4: Native Query

```java
@Query(

 value=
 "SELECT * FROM students",

 nativeQuery=true

)
List<Student> getAllStudents();
```

---

# Step 5: Projection

## StudentView.java

```java
public interface StudentView {

    String getName();

    String getEmail();
}
```

---

Repository

```java
List<StudentView>
findAllProjectedBy();
```

---

Flow

```txt
Database

id
name
email
phone
address

      │

      ▼

Projection

      │

      ▼

name
email
```

---

# Step 6: Pagination

Repository

```java
Page<Student>
findAll(Pageable pageable);
```

---

Controller

```java
@GetMapping
public Page<Student> getAll(

      Pageable pageable

){

   return repository
          .findAll(pageable);
}
```

---

Request

```http
/students?page=0&size=10
```

---

# Pagination Flow

```txt
100000 Rows

      │

      ▼

Pageable

      │

      ▼

10 Rows

      │

      ▼

Response
```

---

# Step 7: Sorting

Request

```http
/students?page=0
&size=10
&sort=name,asc
```

Generated SQL

```sql
ORDER BY name ASC
```

---

# Step 8: Specification

Repository

```java
@Repository
public interface StudentRepository

extends JpaRepository<Student,Long>,

JpaSpecificationExecutor<Student>{

}
```

---

## StudentSpecification.java

```java
public class StudentSpecification {

    public static
    Specification<Student>
    hasName(String name){

        return (
            root,
            query,
            cb
        ) -> cb.equal(
                root.get("name"),
                name
        );
    }
}
```

---

# Dynamic Search Flow

```txt
Request

name=ashraful
age=25

       │

       ▼

Specification

       │

       ▼

Criteria API

       │

       ▼

JPQL

       │

       ▼

SQL

       │

       ▼

Database
```

---

# N+1 Problem

Bad Example

```java
List<Department>
departments =
repository.findAll();
```

Then:

```java
department.getEmployees();
```

Produces:

```txt
1 Query

Departments

+

50 Queries

Employees
```

---

# Solution

```java
@Query("""
SELECT d
FROM Department d
JOIN FETCH d.employees
""")
```

---

# Indexing

Create Index

```sql
CREATE INDEX
idx_student_email
ON students(email);
```

---

# Execution Plan

```sql
EXPLAIN ANALYZE

SELECT *
FROM students
WHERE email='abc@gmail.com';
```

---

# Performance Tuning Flow

```txt
Request

   │

   ▼

Repository

   │

   ▼

JPQL

   │

   ▼

Hibernate

   │

   ▼

SQL

   │

   ▼

Index Scan

   │

   ▼

Execution Plan

   │

   ▼

PostgreSQL
```

---

# Interview Questions

### Validation

```txt
What is @Valid?

Difference between
@NotNull
@NotBlank
@NotEmpty?

Why use DTO?

How does Bean Validation work?

What is MethodArgumentNotValidException?
```

### Query Layer

```txt
Difference between
JPQL and Native Query?

What is Projection?

What is Pagination?

What is Specification?

What is Criteria API?

What is N+1 Problem?

What is JOIN FETCH?

How does Index improve performance?

How do you analyze slow queries?
```

---

# Phase 5 Master Checklist ✅

### Validation

* [ ] DTO
* [ ] @Valid
* [ ] @NotBlank
* [ ] @NotNull
* [ ] @Email
* [ ] @Size
* [ ] @Min
* [ ] @Max
* [ ] Validation Flow
* [ ] GlobalExceptionHandler

### Query Layer

* [ ] Derived Query
* [ ] JPQL
* [ ] @Query
* [ ] Native Query
* [ ] Projection
* [ ] Pagination
* [ ] Sorting
* [ ] Specification
* [ ] Criteria API
* [ ] Dynamic Search
* [ ] N+1 Problem
* [ ] JOIN FETCH
* [ ] Indexing
* [ ] Execution Plan
* [ ] Performance Tuning

This format (Project Structure → ASCII Architecture → Step-by-Step Coding → Request Flow → Interview Questions → Checklist) is the best template to follow from **Phase 6 onward** as well.

